package trees

// Traverse the tree using BFS or DFS.

// Use a map to store each node’s parent.

// Once both p and q are found, construct the path from each to the root using the parent map.

// Traverse upwards from both to find the first common node → that’s the LCA.
